<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/android:windowBackground"
    android:fitsSystemWindows="true"
    tools:context=".MainActivity">

    <!-- AppBar dengan Style Khusus -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/topAppBar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="NoteIT!"
            app:menu="@menu/top_app_bar_menu"
            style="@style/Widget.App.Toolbar" />
        <!-- SEMUA atribut warna manual seperti app:titleTextColor dan android:background DIHAPUS dari sini, karena sudah dihandle oleh style -->

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Wadah Fragment (Tidak Berubah) -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_container"
        android:name="com.example.note.HomeFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:layout_marginBottom="95dp"/>

    <!-- Bottom Navigation View dengan Style Khusus -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:menu="@menu/bottom_nav_menu"
        style="@style/Widget.App.BottomNavigationView"/>
    <!-- SEMUA atribut warna manual seperti android:background, app:itemIconTint, dan app:itemTextColor DIHAPUS dari sini, karena sudah dihandle oleh style -->


    <!-- FAB (Sesuaikan margin bottom jika perlu) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabMain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@android:drawable/ic_input_add"
        app:tint="@android:color/white"
        app:backgroundTint="#6D4C41"
        android:contentDescription="Tambah Catatan"
        android:visibility="gone"
        tools:visibility="visible"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="110dp"/>
    <!-- Saya kembalikan margin ke 90dp, karena 110dp mungkin terlalu tinggi. Sesuaikan lagi jika perlu. -->

</androidx.coordinatorlayout.widget.CoordinatorLayout>
